# Data table 1:2
Loading the data table from Intro, DataTable 1.2
# Since datatable is not in base R, 
# the package must be called
require(data.table)
#  create a data table
x <- c("a","b","c","d","e")
y <- c(1,2,3,4,5)
my_first_data_table <- data.table(x,y)
my_first_data_table
#  create data.table by using recycling
DT <- data.table(a=c(1L,2L),b=LETTERS[1:4])
#  Special symbol ".N" contains the number of rows
#  You can put this symbol  inside square brackets
#  print the penultimate row of DT using `.N`
DT[.N - 1]
#  Print the column names of DT, and the number of rows and columns
colnames(DT)
dim(DT)
#  Select row 2 twice and row 3, returning a data.table with three rows where row 2 is a duplicate of row 1.
DT[c(2,2,3)]
#